# Task Log: 2026-07

## 2026-07-28 — Single-city Lviv architecture and frontend implementation

**Agents:** project orchestrator, `nextjs-ssg-architect`, `sanity-groq-specialist`, `react-next-component-specialist`, `code-reviewer`
**Task:** Consolidate the complete Lviv festival experience onto `/`, remove multi-city route/theme UI, and preserve static export.

### Summary

Accepted ADR 0006 and completed the single-city route merge. The homepage now fetches the full Sanity `festivalCity` document at stable slug `lviv`, renders CMS-backed locations/artists/partners/body in the fixed Heroic theme, and uses a static cinematic Lviv Hero. The dynamic root slug route now handles generic `page` documents only; legacy city slugs are reserved, festival-city sitemap entries are removed, and Sanity internal links to `festivalCity` resolve to `/`. Obsolete multi-city queries and the duplicate city-detail component were removed.

### Files changed

| File | Change |
| --- | --- |
| `frontend/components/landing/LandingExperience.tsx` | Removed `"use client"`, changed prop from `cities: LANDING_CITIES_QUERY_RESULT` to `city: FESTIVAL_CITY_QUERY_RESULT \| null`, applied `theme="heroic"`, passes locations/artists/partners/body to child sections, renders optional `city.body` via PortableTextRenderer in a semantic section |
| `frontend/components/landing/Hero.tsx` | Full rewrite: removed `"use client"`, city cards, theme maps, and city links; accepts nullable `city`; renders static Lviv Hero with one `h1`, approved Ukrainian fallback copy (title, tagline, city, dates, venue, founder), optional Sanity hero image as sole priority LCP image with responsive `sizes`, `motion-reduce`-safe effects |
| `frontend/components/landing/Header.tsx` | Removed МІСТА nav link; remaining anchors target `#program`, `#artists`, `#partners` |
| `frontend/components/landing/LocationsGrid.tsx` | Full rewrite: accepts `locations` array from CMS; removed hardcoded Epic Stage and all hardcoded location arrays; icon mapping by `stageType` and name keywords; empty state when no locations |
| `frontend/components/landing/ArtistsLineup.tsx` | Rewritten: accepts `artists` array directly; removed multi-city flatten/dedup, city name labels, and `/lviv/` links; renders genre instead of city context |
| `frontend/components/landing/PartnersSection.tsx` | Rewritten: accepts `partners` array directly; removed multi-city flatten/dedup and city-name labels; level labels remain |
| `.stitch/DESIGN.md` | Updated: replaced multi-city toggle/city-card requirements with fixed Heroic Lviv rules; documented approved active locations; removed canceled Epic Stage; noted single h1, reduced-motion, server-render constraints |
| `frontend/app/page.tsx` | Fetches the canonical Lviv festival document and uses Lviv-specific metadata |
| `frontend/app/(main)/[slug]/page.tsx` | Generates generic pages only and excludes reserved festival slugs |
| `frontend/app/sitemap.ts` | Excludes festival-city and reserved city URLs |
| `frontend/sanity/queries/**`, `frontend/sanity/lib/fetch.ts`, `frontend/sanity.types.ts` | Removed obsolete multi-city queries/helpers, mapped festival-city links to `/`, and regenerated types |
| `frontend/lib/festival-themes.ts`, `frontend/lib/reserved-root-slugs.ts` | Made Heroic the default and centralized reserved route slugs |
| `frontend/components/festival-city/festival-city-page.tsx` | Removed obsolete duplicate city route UI |
| `docs/adr/0006-single-city-lviv-root-route.md`, `docs/plans/single-city-lviv-pivot.md` | Recorded and executed the approved architecture |
| `artifacts/SANITY_SCHEMAS.md` | Corrected the current content rebuild workflow to manual GitHub Actions dispatch |

### Verification performed

- `pnpm typegen` — regenerated 68 schema types and 9 query types
- `pnpm --filter frontend typecheck` — passes
- `pnpm --filter studio typecheck` — passes
- `pnpm --filter frontend lint` — passes
- `NEXT_PUBLIC_SITE_URL=https://example.com NEXT_PUBLIC_SITE_ENV=production pnpm --filter frontend build` — compiles and exports successfully (8 routes)
- `frontend/out/index.html` contains: Львів (10×), Країна Мрій (26×), 15–16 серпня (6×), Богдана Хмельницького (4×), Парк культури (2×)
- Exactly 1 `<h1` in static output
- No "МІСТА" text in static output
- No "cities" references in static output
- `frontend/out/lviv/` absent
- `frontend/out/kamianets/` absent
- `frontend/out/tickets/` present
- `frontend/out/sitemap.xml` contains `/` and `/tickets/` only for the current dataset; no `/lviv/` or `/kamianets/`
- Final `code-reviewer` pass — no remaining blocker, high, or medium findings

### Risks

| Risk | Status |
| --- | --- |
| Lviv content incomplete or unavailable at build time | Mitigated: fallback Hero copy and section empty states keep `/` exportable |
| Artist schema lacks day/time field | Accepted limitation: renders ordered lineup only; no per-day grouping |
| Stable `lviv` slug changes | Requires a coordinated content/code update; documented in ADR 0006 |
| Existing external `/lviv/` links | Route is intentionally absent; add a host redirect only if launch analytics show a need |

### Follow-ups

- Content team to publish Lviv `festivalCity` document with approved locations, artists, partners, and hero image
- Browser-smoke-test the root anchors, ticket route, 404, and content after the final Sanity publication and staging deploy
- Future: add artist performance day/time field if daily schedule grouping becomes editorially required

## 2026-07-28 — Lviv places plan and grid enhancement

**Agents:** project orchestrator, `sanity-schema-architect`, `react-next-component-specialist`, `code-reviewer`, `accessibility-ui-tester`
**Task:** Refactor the approved Lviv locations brief into a project plan and prepare the CMS-driven Program grid for all nine approved location categories.

### Summary

Refactored `docs/plans/places.md` into the standard plan format with the approved nine-location order, descriptions, stage-type guidance, the separate two-day lineup source, risks, and verification steps. Schema review confirmed that the existing `location` documents and ordered `festivalCity.locations[]` references support the task without an ADR, schema, GROQ, or TypeGen change.

Updated `LocationsGrid` without adding a hardcoded content catalog: it continues to render only CMS references, while its bounded presentation resolver now provides meaningful icons and Heroic-theme tones for main stage, crafts, books, wedding, mystical, folk, children, and food locations. The Program section received an accessible name, sticky-header scroll offset, contrast-safe overline, decorative-divider semantics, and non-interactive cards no longer imply click behavior.

### Files changed

| File | Change |
| --- | --- |
| `docs/plans/places.md` | Standardized plan, approved content order, editorial guidance, verification, and pending CMS population steps |
| `frontend/components/landing/LocationsGrid.tsx` | CMS-preserving visual category resolver, nine-category icons/tones, accessible Program anchor/section, and static non-interactive cards |
| `artifacts/CONTENT_LATEST.md` | Added and ordered the missing `Простори Ремесел` location in the approved nine-place catalog |
| `.stitch/DESIGN.md` | Updated the CMS-driven active location catalog to nine places |

### Verification performed

- Schema/query review: existing model supports all nine locations and preserves `festivalCity.locations[]` order.
- `pnpm --filter frontend typecheck` — passes.
- `pnpm --filter frontend lint` — passes.
- `NEXT_PUBLIC_SITE_URL=https://example.com NEXT_PUBLIC_SITE_ENV=production pnpm --filter frontend build` — passes.
- `git diff --check` — passes.
- Focused code review: no blocker or high-priority issue; medium review findings were addressed before final verification.

### Risks and follow-ups

- The current build cannot display nine cards until editors publish the nine `location` documents and add them, in order, to the canonical Lviv `festivalCity` references.
- Editors must enter manual Latin slugs because the schema validates slugs as lowercase Latin/hyphen values while location names are Ukrainian.
- The canceled Epic Stage must remain unreferenced from the Lviv document.
- The artist schedule still cannot be grouped by day without a future performance-schema decision.

## 2026-07-29 — Published Lviv location catalog

**Agents:** project orchestrator and Sanity MCP
**Task:** Populate the published Lviv festival location catalog, attach its ordered references, and verify local/static rendering.

### Summary

After deploying the Studio schema and restoring MCP access, inspected the published `festivalCity(slug=lviv)` document. It contained only two location references. Created the four missing location documents, updated five existing approved records with the accepted names, descriptions, and stage types, and published the canonical Lviv document with all nine references in order. The canceled Epic Stage document was left untouched but is not referenced by Lviv.

### CMS changes

- Created and published: Книжкова Країна Мрій, Країна Містична, Зона народних гулянь, Дитяча галявина.
- Updated and published: Головна сцена Мрій, Алея Майстрів, Простори Ремесел, Весільна Локація, Український етно-фудкорт.
- Replaced `festivalCity(slug=lviv).locations[]` with the approved ordered nine-item reference list.

### Verification performed

- Deployed schema listed by `npx sanity@latest schema list` as `_.schemas.default` for `a5yik2yd / production`.
- Sanity query returned exactly nine ordered location references with the approved titles, descriptions, and stage types.
- The published Lviv location list contains no Epic Stage reference.
- Local Next dev response at `http://localhost:3001/` contains all nine approved titles and no Epic Stage.
- Fresh production-like static export contains all nine approved titles and excludes Epic Stage.

### Follow-ups

- Perform manual desktop/mobile/keyboard smoke testing at `http://localhost:3001/#program`.
- Run the documented GitHub Actions staging rebuild/deploy, then smoke-test the deployed Program section.
- Populate the remaining approved Lviv lineup, partners, and final hero assets in separate content work.

## 2026-07-29 — Published Lviv lineup and accessible homepage programme

**Agents:** project orchestrator, `sanity-schema-architect`, `react-next-component-specialist`, `accessibility-ui-tester`, `code-reviewer`, and Sanity MCP
**Task:** Publish the approved six-artist Lviv lineup, attach the ordered references to the canonical festival document, and render the MVP two-day programme without changing the schema.

### Summary

Published five new artist records, updated the existing `oleg-skrypka-ta-vv` record, and replaced the canonical Lviv `artists[]` references with the approved six-item order. The homepage now derives two explicit day groups from the existing ordered-reference contract: the first three artists render under 15 August and the remaining three under 16 August. This is intentionally documented as an MVP editorial convention, not durable scheduling data.

Implemented the Heroic cards with optional Sanity images, accessible Music-icon no-photo states, genre and truncated biography text, a visible 20th-anniversary Oleg/VV badge, and the approved announcement banner. The final accessibility pass added named nested day sections, sticky-header-safe anchor offsets, stronger placeholder contrast, readable badge text, and safe, announced external links in Portable Text.

### Files changed

| File | Change |
| --- | --- |
| `docs/plans/lineup.md` | Recorded the six-record order, temporary day-group convention, verification, and remaining smoke-test work. |
| `frontend/components/landing/LandingExperience.tsx` | Exports the documented `DAY_1_ARTIST_COUNT = 3` convention and derives the day groups. |
| `frontend/components/landing/ArtistsLineup.tsx` | Added day-group programme UI, optional-image cards, highlight, announcement, semantic sections, and accessible anchor/fallback refinements. |
| `frontend/components/landing/LocationsGrid.tsx` | Adjusted the Program anchor offset for mobile sticky-header navigation. |
| `frontend/components/portable-text-renderer.tsx` | Adds `noreferrer` and Ukrainian screen-reader context for new-tab external links. |
| `docs/ai/PROJECT_LEDGER.md` | Updated content-completion state, risks, priorities, and active plan index. |

### Verification performed

- Sanity query confirmed exactly six published canonical Lviv artist references in the approved order; Oleg/VV was reused rather than duplicated.
- `pnpm typegen` — passes.
- `pnpm --filter frontend typecheck` — passes.
- `pnpm --filter studio typecheck` — passes.
- `pnpm --filter frontend lint` — passes.
- `NEXT_PUBLIC_SITE_URL=https://example.com NEXT_PUBLIC_SITE_ENV=production pnpm --filter frontend build` — passes; static export contains the six names, both day labels, anniversary badge, and announcement copy.
- Local dev response at `http://localhost:3001/` contains the Line-up day label and anniversary badge.
- `git diff --check` — passes.
- Focused code review found no blocker, high, or medium correctness finding.

### Risks and follow-ups

- The reference order remains the source of Day 1/Day 2 grouping; create a schema ADR before adding independently editable performance day, time, or stage data.
- Approved artist photos and external links remain optional and are intentionally absent; no-photo cards will remain until approved assets are provided.
- Perform manual desktop/mobile/keyboard smoke tests for `#artists` and `#program`, then deploy and repeat those checks on staging.

## 2026-07-29 — Prepared poster and festival-history implementation plans

**Agents:** project orchestrator, `tailwind-ui-implementer`, `content-blocks-architect`, `seo-metadata-auditor`, Stitch design skill
**Task:** Prepare separately reviewable plans to restore the official poster’s red/green balance and to make verified festival history a core homepage selling point.

### Summary

Read `artifacts/КРАЇНА МРІЙ 1080х1080.jpg` as the canonical 2026 visual reference and prepared a design plan that treats Brand Red and Natural Green as one active, balanced identity without restoring deleted theme selection. Prepared a separate content-model plan for the seven facts from `artifacts/facts.md`: it proposes a citation-aware, city-owned milestone array and semantic static timeline after Founder.

### Files changed

| File | Change |
| --- | --- |
| `docs/plans/poster-led-brand-rebalance.md` | Draft visual-rebalance plan with section allocation, contrast constraints, validation, and fixed-Heroic guardrails. |
| `docs/plans/festival-history-facts.md` | Draft source-verification, ADR, schema, query, CMS, semantic timeline, and static-export plan. |
| `docs/ai/PROJECT_LEDGER.md` | Added draft plans and the historical-claim verification risk. |

### Architectural impact and follow-ups

- The brand-rebalance plan requires no ADR while it remains a static visual correction inside ADR 0006.
- The history plan requires proposed ADR 0007 before any schema implementation.
- Neither plan is approved or implemented. Product review must confirm visual allocation, source/citation policy, and the proposed placement before work begins.

## 2026-07-29 — Implemented poster-led design, local imagery, and Lviv history

**Agents:** project orchestrator, `sanity-schema-architect`, `tailwind-ui-implementer`, `react-next-component-specialist`, `content-blocks-architect`, `accessibility-ui-tester`, `code-reviewer`, Stitch design skill, Sanity MCP
**Task:** Execute the approved poster-led brand rebalance and the festival-history plan using the supplied `about.md`, expanded `facts.md`, and image assets.

### Summary

Accepted ADR 0007 and deployed the additive `festivalMilestone` schema to the production dataset. Published twelve ordered milestones on the canonical Lviv `festivalCity` document and rendered them in a server-side, semantic History section after Founder. High-risk 1997, 2013, and 2022–2024 statements are visibly qualified as editorial festival material pending public source URLs.

Rebalanced the fixed Heroic homepage around the poster’s integrated red/green identity without restoring a deleted theme or runtime toggle. Natural Green was darkened to AA-safe `#5A6B38`. The supplied stage, bird-mark, and folk-border assets are now direct, optimized WebP landing assets: stage photography supplies the Hero fallback and About image, while the bird mark is explicitly used as heritage art rather than a founder portrait. `about.md` now informs the About and Founder copy without introducing its unverified numerical claims.

### Files changed

| Area | Change |
| --- | --- |
| `docs/adr/0007-festival-history-milestones.md` | Accepted decision for ordered inline city-owned historical milestones. |
| `studio/schemas/objects/festival-milestone.ts`, `studio/schema-types.ts`, `studio/schemas/documents/festival-city.ts`, `studio/schema.json` | Added, registered, extracted, and deployed `festivalMilestone` and `festivalCity.history`. |
| `frontend/sanity/queries/festival-city.ts`, `frontend/sanity.types.ts` | Added typed build-time history projection and regenerated types. |
| `frontend/components/landing/HistoryTimeline.tsx`, `LandingExperience.tsx`, `Header.tsx` | Added semantic timeline, conditional History anchor, and homepage placement. |
| `frontend/components/landing/**`, `frontend/app/globals.css`, `frontend/lib/tailwind-patterns.ts` | Applied poster-led colour balance, AA-safe Green, named sections, anchor offsets, and reduced-motion refinements. |
| `frontend/public/images/festival/*.webp` | Added optimized direct local assets (196 KB Hero stage, 304 KB About stage, 72 KB bird mark, 44 KB folk border). |
| `.stitch/DESIGN.md` | Recorded visual reference, active palette, image usage, and history-section contract. |
| `docs/plans/*.md`, `docs/ai/PROJECT_LEDGER.md` | Updated plan and project state. |

### Verification performed

- `pnpm --filter studio exec sanity schemas deploy --workspace default` — deployed the updated default Studio schema.
- Sanity published query — confirmed exactly 12 ordered Lviv milestones.
- `pnpm typegen` — generated 69 schema types and 9 query types.
- `pnpm --filter frontend typecheck` — passes.
- `pnpm --filter studio typecheck` — passes.
- `pnpm --filter frontend lint` — passes.
- `NEXT_PUBLIC_SITE_URL=https://example.com NEXT_PUBLIC_SITE_ENV=production pnpm --filter frontend build` — passes and retains eight exported routes.
- Static output assertions confirmed history content and all four WebP landing assets in `frontend/out/`.
- `git diff --check` — passes.
- Initial code and accessibility reviews identified contrast, anchor, decorative-image, and reduced-motion issues; all blocker/high findings were fixed and the accessibility re-review found no blocker or high issue.

### Risks and follow-ups

- Perform manual desktop/mobile/keyboard and staging smoke tests for `#history`, `#program`, `#artists`, and `#partners`.
- Add approved public source URLs for the qualified 1997, 2013, and 2022–2024 statements before public deployment if available.
- Publish partners and approved artist imagery; a Sanity hero image can later replace the local Hero fallback.

## 2026-07-30 — Audited refactored campaign visual assets

**Agent:** project orchestrator (no subagents used, by request)
**Task:** Inspect all `artifacts/img/29.07` and `artifacts/img/30.07` visual sources and prepare a Hero-first landing-image integration plan.

### Summary

Reviewed the 29 July archive, all six 30 July portrait campaign compositions, all four mobile art-direction variants, and the illustration contact sheet. The resulting plan selects a small curated asset sequence rather than shipping every poster: 30 July bird artwork for Hero, family for About, heritage art for History, food art for Programme, and folk-dance art for Line-up. The existing 29 July stage Hero fallback is explicitly retired by the proposed plan because product marked it unconfirmed.

The Oleg Skrypka campaign poster is intentionally blocked from implementation until the visible `09.09` date/event context and use approval are confirmed. The 2.1 MB illustration contact sheet is a source for future sliced motifs only and must never ship whole.

### Files changed

| File | Change |
| --- | --- |
| `docs/plans/landing-visual-asset-integration.md` | Added asset inventory, visual roles, Hero decision, performance budgets, provenance gates, and validation plan. |
| `docs/ai/PROJECT_LEDGER.md` | Added the draft plan and audit record. |

### Risks and follow-ups

- Await product confirmation of the curated asset roles, especially the Oleg portrait gate and whether the food/folk-dance supporting visuals should ship in this wave.
- Execution must use optimized derivatives in a new public namespace, preserve semantic event text in HTML, and validate static output and image budgets.

## 2026-07-30 — Integrated approved campaign artwork

**Agent:** project orchestrator with `react-next-component-specialist`, `tailwind-ui-implementer`, `accessibility-ui-tester`, `code-reviewer`, and `performance-build-auditor`
**Task:** Implement and validate the approved 30 July visual-asset plan across the single-city Lviv landing.

### Summary

Added a server-rendered `CampaignArtwork` `<picture>` component and optimized local campaign derivatives. The Hero keeps the approved Sanity-image precedence and uses the responsive bird campaign as its 4:5/9:16 fallback, with semantic event facts and ticket CTA in both branches. Added approved family, Oleg Skrypka, heritage, food, folk-dance, and border visuals to their planned sections without changing CMS ownership of programme or artist data. The refreshed Oleg source was re-reviewed and approved because the earlier conflicting `09.09` detail is absent.

Retired the old stage, bird-mark, and folk-border public derivatives, removed unused motif crops, and re-encoded the shared border to 49,514 bytes. Accessibility follow-up added visible Hero CTAs, complete-art `object-contain`, decorative hiding, modest hover zooms, and reduced-motion scale resets.

### Files changed

- `frontend/components/landing/CampaignArtwork.tsx`
- `frontend/components/landing/Hero.tsx`
- `frontend/components/landing/AboutFestival.tsx`
- `frontend/components/landing/Founder.tsx`
- `frontend/components/landing/HistoryTimeline.tsx`
- `frontend/components/landing/LocationsGrid.tsx`
- `frontend/components/landing/ArtistsLineup.tsx`
- `frontend/components/landing/Footer.tsx`
- `frontend/public/images/festival/30-07/*.webp`
- `.stitch/DESIGN.md`
- `docs/plans/landing-visual-asset-integration.md`
- `docs/ai/PROJECT_LEDGER.md`
- `docs/logs/2026-07.md`

### Verification performed

- `pnpm --filter frontend typecheck` — passes.
- `pnpm --filter frontend lint` — passes.
- `pnpm --filter studio typecheck` — passes.
- `NEXT_PUBLIC_SITE_URL=https://example.com NEXT_PUBLIC_SITE_ENV=production pnpm --filter frontend build` — passes; eight static routes emitted.
- Static output inspection — exactly one active Hero image preload, all supporting campaign images lazy-loaded, all approved derivatives copied, and no retired public image references.
- Accessibility re-review — no blocker/high findings; reduced-motion hover reset follow-up applied.
- Performance review — local derivative budgets and static-export safety pass; shared border is under 50 KB.
- `git diff --check` — passes.

### Architectural impact

No ADR required. The work remains presentation-only, local-static, server-rendered, and within ADR 0006’s root/static-export contract. Sanity continues to own festival content and retains Hero-image precedence.

### Risks and follow-ups

- Complete manual desktop/mobile/keyboard/reduced-motion and staging smoke tests.
- The active Sanity Hero in the current export is a single unresponsive 256 KB JPEG. Consider Sanity CDN responsive variants or an optimized approved replacement before launch if LCP budgets require it.

## 2026-07-30 — Planned festival photo rewind and editorial cleanup

**Agent:** project orchestrator with `react-next-component-specialist`, `tailwind-ui-implementer`, `accessibility-ui-tester`, and `performance-build-auditor`
**Task:** Audit the new rewind photograph set and plan a gallery, programme-content removal, and campaign-image relocation.

### Summary

Reviewed all 30 source photographs in `artifacts/img/30.07/rewind-2014/` (approximately 112 MB), the current landing order, the published Lviv location references, and the bottom CMS `Про фестиваль` body. Recommended an eight-image, server-rendered native scroll-snap gallery rather than shipping every source or introducing another carousel dependency. The proposed set includes `книжкова.jpeg` with a caption linking it to `Книжкова Країна Мрій` without identifying the pictured person.

The audit found a blocking provenance concern: visible contemporary wartime context and sampled metadata conflict with the folder's 2014 label. The plan therefore blocks year-specific copy and public derivatives until product confirms the actual event year, rights, and credit requirements.

The plan also identifies the exact published `Український етно-фудкорт` reference for removal while retaining the standalone location document, and moves `history-rhythm.webp` plus `lineup-community.webp` into a named bottom `Про фестиваль` composition rather than mixing campaign art into the documentary gallery.

### Files changed

- `docs/plans/festival-photo-rewind-gallery.md`
- `docs/ai/PROJECT_LEDGER.md`
- `docs/logs/2026-07.md`

### Verification performed

- Inspected all 30 source images using lightweight temporary review derivatives outside the repository.
- Loaded the deployed `festivalCity` and `location` schemas.
- Queried the published canonical Lviv document and confirmed nine ordered locations, including food-court ID `a1e96ff0-f399-46ad-8c4f-1b5849c28c3b` and `Книжкова Країна Мрій`.
- Queried the published bottom body and confirmed its `Про фестиваль` heading and narrative.
- Reviewed slider architecture, accessibility, and image-performance constraints with specialist agents.

### Architectural impact

No ADR is proposed while the gallery remains local-static, code-curated, server-rendered, and dependency-free. A Sanity-managed archive or shared scripted carousel would require an ADR.

### Risks and follow-ups

- Product subsequently confirmed the set documents the 21–23 June 2024 Kyiv event, the project holds a publication licence, and visible credits are not required.
- Product subsequently approved the eight-image sequence, event copy, legacy public path, and native-dialog lightbox.
- After approval, patch and publish the Lviv location reference array, generate responsive derivatives, and run browser/staging smoke tests.

## 2026-07-30 — Implemented 2024 Kyiv photo rewind and programme cleanup

**Agent:** project orchestrator with `react-next-component-specialist`, `tailwind-ui-implementer`, `accessibility-ui-tester`, `code-reviewer`, and `performance-build-auditor`
**Task:** Implement the licensed 2024 event photo rail/lightbox, remove the Lviv food-court reference, relocate campaign illustrations, rebuild the founder asset, and add verified media context.

### Summary

Converted the approved eight photographs from `artifacts/img/30.07/rewind-2024/` into 600w/1200w WebP derivatives under the product-requested `frontend/public/images/festival/rewind-2014/` path. Added a responsive semantic scroll-snap rail for the 21–23 June 2024 Kyiv event, with event context for the M. M. Hryshko National Botanical Garden, a linked `Суспільне Крим` report about the Crimean stage, and a focused native-dialog lightbox. The lightbox has native Escape/focus containment, Safari-compatible backdrop dismissal, Close/Previous/Next controls, endpoint states, a captioned live counter, and trigger-focus restoration.

Rebuilt `founder-oleg-skrypka.webp` from the updated mobile source. Moved `history-rhythm.webp` and `lineup-community.webp` into the bottom CMS-backed `Про фестиваль` section with distinct accessible descriptions. Removed and published the `Український етно-фудкорт` reference from the canonical Lviv document, retained the standalone location document, removed the food-specific callout and `program-food.webp`, and verified eight active locations with `Книжкова Країна Мрій` retained.

### Files changed

- `frontend/components/landing/FestivalPhotoGallery.tsx`
- `frontend/components/landing/FestivalAboutContent.tsx`
- `frontend/components/landing/LandingExperience.tsx`
- `frontend/components/landing/HistoryTimeline.tsx`
- `frontend/components/landing/ArtistsLineup.tsx`
- `frontend/components/landing/LocationsGrid.tsx`
- `frontend/public/images/festival/rewind-2014/*.webp`
- `frontend/public/images/festival/30-07/founder-oleg-skrypka.webp`
- `frontend/public/images/festival/30-07/program-food.webp` (removed)
- `.stitch/DESIGN.md`
- `docs/plans/festival-photo-rewind-gallery.md`
- `docs/plans/places.md`
- `docs/ai/PROJECT_LEDGER.md`
- `docs/logs/2026-07.md`
- Published Sanity `festivalCity(slug == "lviv")` location references

### Verification performed

- Sanity published query confirms eight locations, no food-court reference, and the book programme retained.
- `pnpm --filter frontend typecheck` — passes.
- `pnpm --filter frontend lint` — passes.
- `pnpm --filter studio typecheck` — passes.
- Clean production-like static export — passes with eight routes.
- Static HTML inspection confirms eight lazy responsive gallery images, one native dialog, one Hero image preload, the `Суспільне Крим` link, and no food-court/program-food output.
- Gallery derivatives total approximately 1.2 MB; the initial dialog contains no image, so full lightbox media is not downloaded before opening.
- Specialist accessibility and code reviews found no blocker/high issue; live slide announcements and campaign-art text alternatives were strengthened.
- `git diff --check` — passes.

### Architectural impact

No ADR required. The gallery is code-curated and static-export compatible; the only client boundary is a small native-dialog lightbox with no new dependency. Sanity schemas and GROQ contracts are unchanged.

### Risks and follow-ups

- Complete manual keyboard, touch, screen-reader, Escape/backdrop, focus-return, 200% zoom, reduced-motion, and staging tests.
- The active Sanity Hero remains a separate unresponsive 256 KB JPEG LCP risk.

### Follow-up: portrait gallery card sizing

Fixed `07-traditional-dance-600.webp` expanding its slider card because its portrait intrinsic ratio participated in layout. Gallery thumbnails now use an overflow-hidden 3:2 frame with absolutely positioned `object-contain` media; the lightbox continues to show the complete portrait at its natural aspect ratio. No image replacement or new derivative was required.

### Follow-up: revised ten-image gallery selection

Product replaced the handpan, craft still life, book-programme portrait, and portrait dance selections with `DSC_3324.jpg`, `DSC_3130.jpg`, `DSC04387.jpg`, and `9R4A8116.jpeg`. Added `Країна мрій - @rybka.in.ua-5892.jpg` and `DSC04639.jpg` as separate performance slides. Generated 600w/1200w WebP variants, updated all affected Ukrainian alt text and captions, and removed the eight superseded derivative files. The gallery now contains ten licensed 2024 photographs.

Frontend typecheck, lint, production static build, accessibility text review, and output assertions pass. Static HTML contains ten lazy responsive gallery images, all six new asset pairs, no superseded paths, one native dialog, and one Hero image preload; the complete gallery derivative set is approximately 1.5 MB.

## 2026-07-31 — Refined 2024 Kyiv photo gallery interaction and lightbox resilience

**Task:** Refine the implemented photo gallery’s header rhythm, caption treatment, rail discoverability, and native-dialog behavior without changing its content or architecture.

**Agent(s):** project orchestrator, `tailwind-ui-implementer`, `accessibility-ui-tester`, `code-reviewer`

### Summary

Aligned the gallery intro/header with the History section’s centered narrow `max-w-4xl` rhythm. Preview captions are intentionally removed visually; detailed Ukrainian alternative text and explicit thumbnail accessible action names remain, and captions continue to appear in the lightbox. The semantic proximity-snap list retains native touch/trackpad scrolling with its scrollbar intentionally hidden. Visible guidance, partial-card/mobile affordance, and desktop-only non-looping Previous/Next controls provide discoverability; controls scroll about one card, update on scroll/resize, preserve disabled endpoints, and use instant scrolling for reduced-motion users.

The native dialog is now near full viewport with a transparent shell/image area, dark translucent backdrop, individually high-contrast control/caption surfaces, and vertical overflow/overscroll containment for short viewports and 200% zoom. Close, Escape, backdrop dismissal, Previous/Next, counter, caption, and focus return remain intact.

### Files changed

- `frontend/components/landing/FestivalPhotoGallery.tsx`
- `frontend/app/globals.css`
- `.stitch/DESIGN.md`
- `docs/plans/festival-photo-rewind-gallery.md`
- `docs/ai/PROJECT_LEDGER.md`
- `docs/logs/2026-07.md`

### Verification

- `pnpm --filter frontend typecheck` — passes.
- `pnpm --filter frontend lint` — passes.
- `NEXT_PUBLIC_SITE_URL=https://example.com NEXT_PUBLIC_SITE_ENV=production pnpm --filter frontend build` — passes; eight static routes emitted.
- `git diff --check` — passes.
- Specialist code and accessibility review — no blocker or high issue after fixes.

### Architectural impact

No dependency, schema, GROQ, image manifest/path, or architecture change. No ADR required.

### Risks/follow-ups

- Manual desktop/mobile/touch/keyboard/screen-reader/200%-zoom/reduced-motion/staging checks remain pending.

## 2026-07-31 — Replaced sixth rewind gallery photograph

**Task:** Replace the makers portrait in the sixth gallery position with the approved shared-dance photograph and keep responsive output consistent.

**Agent:** project orchestrator

### Summary

Matched the supplied photograph to the ignored local source `artifacts/img/30.07/rewind-2024/DSC_3457.jpg`. Rebuilt both existing `04-craft-vendors-600.webp` and `04-craft-vendors-1200.webp` public derivatives so `srcset` and the lightbox use the same photograph, updated intrinsic dimensions, and replaced the former makers description with accurate Ukrainian alternative text and caption. Public filenames remain unchanged for compatibility.

### Files changed

- `frontend/public/images/festival/rewind-2014/04-craft-vendors-600.webp`
- `frontend/public/images/festival/rewind-2014/04-craft-vendors-1200.webp`
- `frontend/components/landing/FestivalPhotoGallery.tsx`
- `docs/plans/festival-photo-rewind-gallery.md`
- `docs/logs/2026-07.md`

### Verification

- 600w derivative: 600×400 WebP, approximately 64 KB.
- 1200w derivative: 1200×799 WebP, approximately 172 KB.
- Visual inspection confirms both derivatives match the supplied photograph.
- Frontend typecheck, lint, production-like eight-route build, and `git diff --check` pass.

### Architectural impact

No ADR required. The gallery manifest, responsive-image contract, static-export architecture, and public paths remain unchanged.

### Risks/follow-ups

- Manual browser and staging checks remain pending.

## 2026-07-31 — Corrected Lviv food-court Programme reference

**Agents:** project orchestrator, Sanity MCP
**Task:** Correct the mistaken removal of the CMS-owned `Український етно-фудкорт` Programme card while retaining the completed removal of its separate static callout and image.

### Summary

Product clarified that the 30 July instruction did not remove the food-court location from the programme. Only the separate food-court-specific static section/callout and `program-food.webp` were intended to be removed. The published food-court location was restored as the ninth and final Lviv Programme card; its existing frontend keyword resolver continues to display the ChefHat food treatment.

### CMS change

- Restored published location `a1e96ff0-f399-46ad-8c4f-1b5849c28c3b` (`Український етно-фудкорт`) to canonical `festivalCity(slug=lviv)` document `e1841ff3-3179-44f8-b4cf-69508aa4fa3b` as the ninth/final `locations[]` reference with key `locfoodcourt1`.
- The retained description is: `Гастрономічна подорож регіонами України: сучасна національна кухня, фермерські продукти, напої та страви локальних виробників.` Its `stageType` remains `other`.
- Published order is now: Main stage, Makers Alley, Book Country, Craft Spaces, Wedding, Mystical, Folk celebrations, Children, Ukrainian ethno-foodcourt.

### Files/content changed

- Published Sanity `festivalCity(slug == "lviv")` location references.
- `docs/plans/places.md`
- `docs/plans/festival-photo-rewind-gallery.md`
- `docs/plans/landing-visual-asset-integration.md`
- `docs/ai/PROJECT_LEDGER.md`
- `docs/logs/2026-07.md`

The separate static food-court callout and `frontend/public/images/festival/30-07/program-food.webp` remain absent. No schema, GROQ, TypeGen, or frontend code change was required.

### Verification performed

- Published Sanity query returns exactly nine ordered location references, including the food-court record as the final item.
- `pnpm --filter frontend typecheck` — passes.
- `pnpm --filter frontend lint` — passes.
- Production-like frontend build — passes; eight routes emitted.
- `frontend/out/index.html` contains the food-court card name and approved description and does not contain `program-food.webp`.
- `git diff --check` — passes.

### Architectural impact

No ADR or code change is required. The correction preserves the existing CMS-owned ordered-reference contract under ADR 0002 and the static-export architecture.

### Follow-up

- Manual browser and staging smoke checks remain pending, including the nine-card Programme layout and the absence of the separate food-court callout.

## 2026-07-31 — Published approved lineup artist images

**Agents:** project orchestrator, Sanity CLI asset upload, Sanity MCP
**Task:** Publish the supplied exact-match artist photographs for the current six-card Lviv lineup without changing its content model or presentation contract.

### Summary

The supplied files were found in ignored local `artifacts/img/30.07/artists/`, rather than the user-referred `artifacts/artists` path. Published the exact matches for КОМУ ВНИЗ and ГАЙДАМАКИ. Oleg/VV already had an image, bringing photo coverage to three of six cards. БРАТИ ГАДЮКІНИ, МИРОСЛАВ КУВАЛДІН та ІБАШ, and БУРДОН retain intentional placeholders. `Медовий полин.JPG` was not attached because no artist by that name is among the six Lviv references.

### CMS changes

- Published artist `978bc169-d634-41cd-80c0-8fdc6704e698` (`КОМУ ВНИЗ`) with asset `image-ff60ca6b889abd94d109bb139f7dd18521153153-800x450-jpg` (original `Кому Вниз.jpg`, 800×450) and Ukrainian alt text: `Учасники гурту «КОМУ ВНИЗ» у темному одязі та сонцезахисних окулярах.`
- Published artist `271e999b-1349-4655-983d-8ecf892a24c8` (`ГАЙДАМАКИ`) with asset `image-a88871abd551337a136bb8070451040223f1b35e-790x526-jpg` (original `Гайдамаки.jpg`, 790×526) and Ukrainian alt text: `Учасники гурту «ГАЙДАМАКИ» з музичними інструментами на темному тлі.`

### Verification performed

- Published Sanity query verified the exact artist IDs, original filenames, asset dimensions, and Ukrainian alternative text.
- `pnpm --filter frontend typecheck` — passes.
- `pnpm --filter frontend lint` — passes.
- Production-like frontend build — passes; eight routes emitted.
- Static output contains both new Sanity asset hashes and `alt="Фото артиста КОМУ ВНИЗ"` / `alt="Фото артиста ГАЙДАМАКИ"`.
- `git diff --check` — passes.

### Architectural impact

No schema, GROQ, TypeGen, component, or lineup-order change. The update uses the existing optional artist-photo field and preserves the static-export contract.

### Follow-up

- Complete manual browser and staging crop/responsive checks for the two new images.
- Keep the three remaining no-photo cards as intentional placeholders until exact approved assets are available.

## 2026-07-31 — Narrowed the homepage Sanity type boundary

**Agents:** project orchestrator, `react-next-component-specialist`, `sanity-groq-specialist`, `code-reviewer`
**Task:** Remove obsolete multi-city fields from the generated homepage query result and decouple the Hero from the full Sanity result type.

### Summary

Added a small `HeroContent` presentation contract backed by the shared Sanity image type and moved query-to-Hero mapping into `LandingExperience`. Removed the unused top-level `slug` and `themeKey` projections from `FESTIVAL_CITY_QUERY`, then regenerated `frontend/sanity.types.ts`. The retained `festivalCity` schema and its legacy `epic` token remain unchanged under ADR 0006.

### Files changed

- `frontend/components/landing/Hero.tsx`
- `frontend/components/landing/LandingExperience.tsx`
- `frontend/sanity/queries/festival-city.ts`
- `frontend/sanity.types.ts`
- `docs/plans/homepage-sanity-type-boundary.md`
- `docs/ai/PROJECT_LEDGER.md`
- `docs/logs/2026-07.md`

### Verification performed

- `pnpm typegen` — passes; generated 69 schema types and 9 query types.
- `pnpm --filter frontend typecheck` — passes.
- `pnpm --filter frontend lint` — passes.
- `NEXT_PUBLIC_SITE_URL=https://example.com NEXT_PUBLIC_SITE_ENV=development pnpm --filter frontend build` — passes; eight static routes emitted.
- `git diff --check` — passes.
- Specialist review found no blocker in the scoped type/query cleanup.

### Architectural impact

No new ADR required. This is a local query-projection and presentation-boundary refinement consistent with ADR 0006; schemas, published content, routes, static generation, and deployment remain unchanged.

### Risks and follow-ups

- The separate current removal of the ticket CTA from the active Sanity-image Hero branch conflicts with the earlier visual-integration plan and needs product confirmation; it was preserved rather than changed during this refactor.
- Use the root `pnpm typegen` command. The frontend-local `pnpm --filter frontend typegen` script cannot currently resolve the Sanity CLI from that workspace.

## 2026-07-31 — Integrated approved partner logos

**Agents:** project orchestrator, `performance-build-auditor`, `react-next-component-specialist`, `accessibility-ui-tester`, `code-reviewer`
**Task:** Convert the supplied partner marks to WebP and populate the homepage Partners section without inventing missing editorial metadata.

### Summary

Processed the five supplied marks from `artifacts/img/30.07/partners/` into bounded public WebP assets. Added a static fallback catalog for Concert.ua, Радіо Львівська хвиля, So Good Company, Work.ua, and Новосад і Компанія. The fallback appears only when Sanity returns no partners; any published CMS partner array remains authoritative for names, order, levels, URLs, and logos.

The cards share one normalized server-rendered path, retain visible partner-name headings, use decorative logo alternatives to avoid duplicate announcements, and explicitly preserve list semantics. No URLs or partnership levels were inferred from filenames or artwork.

### Files changed

- `frontend/components/landing/PartnersSection.tsx`
- `frontend/public/images/festival/30-07/partners/concert-ua.webp`
- `frontend/public/images/festival/30-07/partners/radio-lvivska-khvylia.webp`
- `frontend/public/images/festival/30-07/partners/so-good-company.webp`
- `frontend/public/images/festival/30-07/partners/work-ua.webp`
- `frontend/public/images/festival/30-07/partners/novosad-and-company.webp`
- `docs/plans/partner-logo-integration.md`
- `docs/ai/PROJECT_LEDGER.md`
- `docs/logs/2026-07.md`

### Verification performed

- Visual inspection of all five WebP derivatives.
- Combined derivative size: approximately 27.2 KB.
- `pnpm --filter frontend typecheck` — passes.
- `pnpm --filter frontend lint` — passes.
- `NEXT_PUBLIC_SITE_URL=https://example.com NEXT_PUBLIC_SITE_ENV=development pnpm --filter frontend build` — passes; eight static routes emitted.
- Static output contains all five logo files, partner names, and image paths.
- Accessibility review found no critical blocker; explicit list semantics were added for Safari/VoiceOver resilience.
- `git diff --check` — passes.

### Architectural impact

No ADR required. The local catalog is a presentation fallback only; ADR 0002's Sanity-owned partner references remain authoritative whenever populated.

### Risks and follow-ups

- Fallback cards have no links or partnership-level labels because those facts were not supplied.
- If editors publish CMS partners, publish the complete desired set and order because CMS content replaces rather than merges with the fallback.
- Complete manual mobile/desktop and Safari/VoiceOver checks before staging sign-off.

## 2026-07-31 — Replaced the Hero with an interactive two-poster composition

**Agents:** project orchestrator, `react-next-component-specialist`, `sanity-groq-specialist`, `performance-build-auditor`, `accessibility-ui-tester`, `code-reviewer`
**Task:** Retire the active Sanity/current Hero artwork and replace it with the approved community and bird campaign posters.

### Summary

The root Hero now always uses a fixed local two-poster composition. `lineup-community.webp` starts as the foreground and sole eager/high-priority LCP candidate. A new 480×600 `post-poster-2-preview.webp` supplies the partially obscured rear card for 29.8 KB; the full 1440×1800 `post-poster-2.webp` is absent from initial HTML and requested only when a visitor activates the poster swap.

A concurrent product edit made the composition interactive. It was consolidated into one keyboard-operable button instead of two overlapping focus targets. The control exposes complete dynamic foreground/background descriptions with `aria-describedby`, keeps nested images presentational, has an offset focus ring, and disables transitions for reduced-motion users. The external ticket CTA and its new-tab announcement remain intact.

Removed `heroImage` from the narrow Hero presentation model and homepage GROQ projection, then regenerated TypeGen output. The `festivalCity.heroImage` schema field remains available; this is a homepage presentation/query pivot, not a content-model deletion.

### Files changed

- `frontend/components/landing/Hero.tsx`
- `frontend/components/landing/LandingExperience.tsx`
- `frontend/sanity/queries/festival-city.ts`
- `frontend/sanity.types.ts`
- `frontend/public/images/festival/30-07/post-poster-2-preview.webp`
- `.stitch/DESIGN.md`
- `docs/plans/landing-visual-asset-integration.md`
- `docs/plans/homepage-sanity-type-boundary.md`
- `docs/ai/PROJECT_LEDGER.md`
- `docs/logs/2026-07.md`

### Verification performed

- `pnpm typegen` — passes; generated 69 schema types and 9 query types.
- `pnpm --filter frontend typecheck` — passes.
- `pnpm --filter frontend lint` — passes.
- `NEXT_PUBLIC_SITE_URL=https://example.com NEXT_PUBLIC_SITE_ENV=development pnpm --filter frontend build` — passes; eight static routes emitted.
- Static output contains one initial community image and one compact rear preview, exactly one eager/high-priority Hero image, no full bird poster, and no retired `hero-traditions-*` references.
- Final performance and accessibility reviews found no blocker or high issue after the single-control and preview fixes.
- `git diff --check` — passes.

### Architectural impact

No new ADR required. The change adds one local `useState` interaction and narrows the homepage projection, but preserves static export, the canonical Lviv content model, and the retained Sanity image schema field.

### Risks and follow-ups

- Manually verify poster swapping with keyboard and screen readers, the offset focus ring, reduced motion, and 320px/400% reflow.
- With cache disabled, confirm `post-poster-2.webp` is not requested before activation and is requested once after the first swap.
- `lineup-community.webp` remains referenced by a dormant bottom-content illustration manifest; avoid rendering both placements simultaneously without a deliberate visual review.

## 2026-07-31 — Updated staging deploy checks for single-city output

**Agents:** project orchestrator
**Task:** Fix the manual staging deploy workflow after the redeploy failed on a stale multi-city export assertion.

### Summary

Triggered manual staging redeploy `30596051829`; build, typecheck, lint, and static export succeeded, but the workflow failed before SSH deployment because `Verify static export output` still required `frontend/out/kamianets/index.html`. The current single-city Lviv architecture exports `/`, `/lviv/`, `/tickets/`, and the placeholder blog route only. Updated the staging workflow to remove Kamianets from the required route-file assertion and from the known-route HTTPS trailing-slash rewrite list.

### Files changed

- `.github/workflows/deploy-staging.yml`
- `docs/logs/2026-07.md`

### Verification performed

- Failed GitHub Actions run inspected: build succeeded and deploy was skipped after stale route verification failed.
- `git diff --check` — passes.

### Risks and follow-ups

- Push the workflow fix, rerun the manual staging deployment, and verify the deployed `/`, `/lviv/`, `/tickets/`, custom 404, and browser console state.
